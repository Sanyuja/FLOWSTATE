# Core AI
anthropic
openai

# Data / Excel
pandas
openpyxl
xlsxwriter

# Media / image
pillow

# Utilities
tqdm
python-dotenv
requests

# ── Content Intelligence layer (content_tagger.py) ────────────────────────────
# Heavier deps — install separately if using the content intelligence pipeline:
#
#   pip install open_clip_torch torch torchvision mediapipe opencv-python
#
# For GPU acceleration (optional, 10-50x faster on large libraries):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
